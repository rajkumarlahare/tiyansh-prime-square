"use client";
import { useState } from "react";
import { KeyRound } from "lucide-react";
export default function ChangePasswordForm({email}:{email:string}){const [password,setPassword]=useState(""),[confirmPassword,setConfirmPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/admin/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password,confirmPassword})});const data=await response.json();if(!response.ok)throw new Error(data.error);location.replace("/admin")}catch(value){setError(value instanceof Error?value.message:"Password change nahi hua")}finally{setBusy(false)}}return <main className="login-page"><section className="login-card"><div className="login-icon"><KeyRound/></div><h1>Naya password banाइए</h1><p>{email}<br/>Temporary password बदलना जरूरी है।</p><form onSubmit={submit}><label><span>NEW PASSWORD</span><div className="login-input"><input type="password" required minLength={12} maxLength={128} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="12+ characters"/></div></label><label><span>CONFIRM PASSWORD</span><div className="login-input"><input type="password" required minLength={12} maxLength={128} autoComplete="new-password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Password फिर लिखें"/></div></label><small>Uppercase, lowercase, number और special character इस्तेमाल करें।</small>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}>{busy?"Saving…":"Save new password"}</button></form></section></main>}
