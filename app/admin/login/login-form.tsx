"use client";
import { useState } from "react";
import { Building2,Eye,EyeOff,LockKeyhole,Mail } from "lucide-react";
import Link from "next/link";
export default function LoginForm({mode,projectId="",projectSlug="",successPath="/admin",changePasswordPath="/admin/change-password"}:{mode:"super"|"client";projectId?:string;projectSlug?:string;successPath?:string;changePasswordPath?:string}){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false),isSuper=mode==="super";async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError("");try{const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password,projectId,projectSlug})}),j=await r.json().catch(()=>({error:"Login failed"}));if(r.ok){location.replace(j.mustChangePassword?changePasswordPath:successPath);return}setError(j.error||"Login failed")}catch{setError("Network error. Dobara try karein.")}finally{setBusy(false)}}return <main className="login-page"><section className="login-card"><div className="login-icon"><Building2/></div><h1>{isSuper?"Rekixo Super Admin":"Client Admin"}</h1><p>{isSuper?<>Central client & project management</>:<>Your project control panel</>}</p><form onSubmit={submit}><label><span>EMAIL ADDRESS</span><div className="login-input"><Mail/><input type="email" required autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} placeholder={isSuper?"owner@rekixo.com":"client@example.com"}/></div></label><label><span>PASSWORD</span><div className="login-input"><LockKeyhole/><input type={show?"text":"password"} required autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password"/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Hide password":"Show password"}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}>{busy?"Signing in…":isSuper?"Sign In as Super Admin":"Sign In to Dashboard"}</button></form>{!isSuper&&<Link className="back-link" href="/">← Back to Project</Link>}</section></main>}
