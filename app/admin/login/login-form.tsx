"use client";
import { useState } from "react";
import { Building2,Eye,EyeOff,LockKeyhole,Mail } from "lucide-react";
import Link from "next/link";
export default function LoginForm(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError("");try{const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(r.ok){location.replace("/admin");return}const j=await r.json().catch(()=>({error:"Login failed"}));setError(j.error||"Login failed")}catch{setError("Network error. Dobara try karein.")}finally{setBusy(false)}}return <main className="login-page"><section className="login-card"><div className="login-icon"><Building2/></div><h1>Tiyansh Admin</h1><p>Project: <b>The Prime Square</b></p><form onSubmit={submit}><label><span>EMAIL ADDRESS</span><div className="login-input"><Mail/><input type="email" required autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@tiyansh.local"/></div></label><label><span>PASSWORD</span><div className="login-input"><LockKeyhole/><input type={show?"text":"password"} required autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password"/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Hide password":"Show password"}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}>{busy?"Signing in…":"Sign In to Dashboard"}</button></form><Link className="back-link" href="/">← Back to Project</Link></section></main>}
